<Window x:Class="ByteFlood.UI.AddRSSFeed"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add new feed" Height="350" Width="500"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <RoutedUICommand x:Key="Commands.Add"/>
        <RoutedUICommand x:Key="Commands.Cancel"/>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="{StaticResource Commands.Add}" Executed="Commands_Add" />
        <CommandBinding Command="{StaticResource Commands.Cancel}" Executed="Commands_Cancel" />
    </Window.CommandBindings>

    <Canvas>

        <TextBlock Text="Feed url:" FontSize="16" Canvas.Left="10" Canvas.Top="20" />
        <TextBox Text="{Binding Url}" Height="25" Width="371" Canvas.Left="113" Canvas.Top="16"/>

        <TextBlock Text="Custom Alias:" FontSize="16" Canvas.Left="10" Canvas.Top="59" />
        <TextBox Text="{Binding CustomAlias}" Height="25" Width="371" Canvas.Left="113" Canvas.Top="55"/>
        <TextBlock Text="(Leave empty for default)" FontSize="10" Canvas.Left="10" Canvas.Top="82" />

        <GroupBox Header="Options" FontSize="17" Height="173" Width="474" Canvas.Left="10" Canvas.Top="100">
            <Canvas>
                <CheckBox IsChecked="{Binding AutoDownload}" Content="Automatically download new items" FontSize="13" Canvas.Left="10" Canvas.Top="10" Width="442"/>
                <TextBlock Text="Filter expression:" FontSize="13" Canvas.Left="9" Canvas.Top="42" Height="22" Width="97" />
                <TextBox Text="{Binding FilterExpression}" Height="25" Width="341" Canvas.Left="111" Canvas.Top="39"/>
                <TextBlock Text="(filter expression will run on torrent names)" FontSize="10" Canvas.Left="10" Canvas.Top="64" />

                <TextBlock Text="Filter action:" FontSize="13" Canvas.Left="10" Canvas.Top="93" Height="22" Width="97" />
                <ComboBox SelectedIndex="{Binding FilterAction}" Height="27" Width="154" Canvas.Left="112" Canvas.Top="88"
                          FontSize="13">
                    <ComboBox.Items>
                        <ComboBoxItem Content="Download"/>
                        <ComboBoxItem Content="Skip"/>
                    </ComboBox.Items>
                </ComboBox>
            </Canvas>
        </GroupBox>

        <Button Content="Cancel" Height="28" Width="80" Canvas.Left="404" Canvas.Top="279" Command="{StaticResource Commands.Cancel}" />
        <Button Content="Add" Height="28" Width="80" Canvas.Left="319" Canvas.Top="279" Command="{StaticResource Commands.Add}" />
    </Canvas>
</Window>
